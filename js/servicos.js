const inputDate = document.querySelector("#entrada_servico");

inputDate.addEventListener("keydown", function (e) {
  e.preventDefault();
});

const inputData = document.querySelector("#saida_servico");

inputData.addEventListener("keydown", function (e) {
  e.preventDefault();
});